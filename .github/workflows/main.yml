name: Testing

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # - name: Checkout repo
      #   uses: actions/checkout@v3
        
      - uses: ./
        with:
          name: Sadra
          age: 22
